{
  "name": "pbs-schedule",
  "version": "1.0.0",
  "scripts": {
    "submit": "gcloud builds submit --tag gcr.io/pbsfm-flutter/pbs-schedule",
    "deploy": "gcloud run deploy pbs-schedule --allow-unauthenticated --platform managed --image gcr.io/pbsfm-flutter/pbs-schedule"
  }
}